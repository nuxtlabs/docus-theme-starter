/**
 * From here you can specify all the configurations that you want to use for your theme.
 * 
 * You can access these via `const { theme } = useDocus()` anywhere in your app.
 * 
 */
export default {
  themeName: '@docus/theme-starter'
}
